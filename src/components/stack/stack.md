# Stack

Chrome-only navigation bar API (Expo Router / SwiftUI–style naming). TanStack Router owns routes; `Stack.*` slots configure header chrome. See source `stack.types.ts` for the v1 web prop subset.

[Full documentation](../../../content/docs/grid-list.mdx) *(add dedicated Stack MDX when ready)*
